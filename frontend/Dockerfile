
FROM node:18 as builder
WORKDIR /app
COPY . .
RUN npx create-react-app temp && cp -r temp/* . && rm -rf temp
RUN npm install && npm run build

FROM nginx:alpine
COPY --from=builder /app/build /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
